server {
  listen 80 default_server;
  listen [::] 80 default_server;

  location /static {
    alias /var/opensand/opensand-conf/static;
  }

  location / {
    uwsgi_pass unix:///var/run/uwsgi/opensand.socket;
    include uwsgi_params;
  }
}
