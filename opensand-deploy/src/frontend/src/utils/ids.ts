export const quitusPrintId = () => "quitus-print-only";

export const quitusWorkId = (record: number) => `quitus-${record}-work`;

export const quitusCommentId = (record: number) => `quitus-${record}-comment`;

export const quitusSignatureId = (record: number) => `quitus-${record}-signature`;
