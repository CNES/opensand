Introduction
------------

This piece of software is the environment plane libraries and binaries for the
satellite eulation testbed Platine

License
-------

Proprietary license.


Library
-------

The sources are in the src subdirectory. The sources build libraries and binaries
that handle Error, Probes and Events. See the INSTALL file to learn how to build
the libraries.
To compile an application using the Platine environment plane library use the
following expression:
gcc `pkg-config platine_env_plane --cflags` `pkg-config platine_env_plane --libs` -o myappli myappli.c


References
----------

