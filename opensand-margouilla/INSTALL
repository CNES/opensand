Library
-------

Configure the library:

 $ ./autogen.sh --prefix=/path/to/installation/directory

Build the library:

 $ make all

Install the library:

 $ make install

The libraries are now located in the /path/to/installation/directory/lib
directory and the header files in the /path/to/installation/directory/include/opensand_conf
directory.


See the installation manual on OpenSAND website (www.opensand.org) for more details
