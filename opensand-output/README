Introduction
------------

This piece of software is the output libraries and binaries for the
satellite emulation testbed OpenSAND

License
-------

see COPYING


Library
-------

The sources are in the src subdirectory. The sources build libraries and binaries
that handle Error, Probes and Events. See the INSTALL file to learn how to build
the libraries.
To compile an application using the OpenSAND output library use the
following expression:
gcc `pkg-config opensand_output --cflags` `pkg-config opensand_output --libs` -o myappli myappli.c


References
----------

