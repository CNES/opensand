Introduction
------------

This piece of software is the configuration library for the satellite
emulation testbed Platine

License
-------

Proprietary license.


Library
-------

The sources are in the src subdirectory. The sources build a library
that allow Platine configuration files parsing. See the INSTALL file to
learn how to build the libraries.
To compile an application using the platine configuration library use the
following expression:
gcc `pkg-config platine_conf --cflags` `pkg-config platine_conf --libs` -o myappli myappli.c


References
----------


