# default sysctl configuration values for OpenSAND, used by
# /usr/libexec/opensand/host_init script loaded in opensand-daemon service
# you may encounter problems with OpenSAND if you change these values

# whether the sysctl commands will be used,
# if false, the following parameters won't be used
SYSCTL="true"

# Forwarding: necessary to be able to forward satellite trafic toward
# workstations
IP_FORWARD="1"
FORWARDING="1"

# The DHCP client command concatenated with interface name to automatically set
# up the address when DHCP is enabled
DHCP_CLIENT_CMD="dhclient"
