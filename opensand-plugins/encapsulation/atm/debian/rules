#!/usr/bin/make -f
# -*- makefile -*-
# Sample debian/rules that uses debhelper.
# This file was originally written by Joey Hess and Craig Small.
# As a special exception, when this file is copied by dh-make into a
# dh-make output file, you may use that output file without restriction.
# This special exception was added by Craig Small in version 0.37 of dh-make.

# Uncomment this to turn on verbose mode.
#export DH_VERBOSE=1

export PYTHON2=$(shell pyversions -vr)

%:
	dh  $@

override_dh_strip:
	dh_strip --dbg-package=libopensand-atm-aal5-encap-plugin-dbg
	
override_dh_install:
	dh_install
	mkdir -p $(CURDIR)/debian/libopensand-atm-aal5-encap-plugin-manager/usr/lib/python$(PYTHON2)/dist-packages/opensand_manager_core/modules/
	install -m 0644 src/atm_aal5.py $(CURDIR)/debian/libopensand-atm-aal5-encap-plugin-manager/usr/lib/python$(PYTHON2)/dist-packages/opensand_manager_core/modules/atm_aal5.py

override_dh_pysupport:
